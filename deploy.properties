# bamboo: ${bamboo.inject.build.mvngoal} -U -Dversion.number=${bamboo.inject.build.version.number}.b${bamboo.buildNumber} -Dversion.postfix=${bamboo.inject.build.version.postfix} -Dbuild_MIKATAAON=${bamboo.inject.build.version.postfix} -Dbranch=${bamboo.planRepository.branchName} -Drevision=${bamboo.repository.revision.number}

deploy.host=luokka
build.version.number=10.0
# postfix develissä "-SNAPSHOT", muualla tyhjä
build.version.postfix=-SNAPSHOT
test.baseUrl=https://itest-virkailija.oph.ware.fi
# build.mvngoal should be 'deploy' at least in prod-branch, otherwise 'install'
build.mvngoal=install
